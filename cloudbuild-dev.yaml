# cloudbuild.yaml dev
steps:
  # build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: [ 'build', '-t', 'gcr.io/$PROJECT_ID/react-docker-google-cloud-dev', '.' ]
  # push the container image
- name: 'gcr.io/cloud-builders/docker'
  args: [ 'push', 'gcr.io/$PROJECT_ID/react-docker-google-cloud-dev']
  # deploy to cloud run on GKE
- name: "gcr.io/cloud-builders/gcloud"
  args: ['run', 'deploy', 'react-docker-google-cloud-dev', '--image', 'gcr.io/$PROJECT_ID/react-docker-google-cloud-dev', '--cluster', 'cloudrun-cicd-cluster', '--cluster-location', 'asia-southeast1-a', '--namespace', 'default', '--platform', 'gke']
timeout: 990s  